@model List<string>

@if (Model != null && Model.Any())
{
    <div class="alert alert-danger my-4">
        <ul>
            @foreach (var error in Model)
            {
                <li>@error</li>
            }
        </ul>
    </div>
}